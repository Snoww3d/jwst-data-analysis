# Lightweight dependencies for MAST proxy service
# (no heavy scientific computing packages)

# Web framework
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.13.4

# Scientific computing (lightweight subset)
numpy==2.2.6
pandas==2.3.3

# Astronomy libraries (search + download only)
astropy==6.1.7
astroquery==0.4.11

# Caching
cachetools>=7.1.4,<8.0.0

# HTTP clients
requests==2.34.2
aiohttp==3.14.1
aiofiles==25.1.0

# AWS (for S3 downloads)
boto3>=1.43.36
